/*** @author admin*/// Cleands.Companies.drop();ds.Employees.drop();//Populate Companiesfor (var i = 1; i < 100; i++){	var comp = new ds.Companies();	comp.name = "Company_"+i;	comp.save();}//Populate Employeesds.Companies.forEach(function(comp_entity){	for (var i = 1; i < 100; i++)	{		var emp = new ds.Employees();		emp.name = "Employee_"+i+" of " + comp_entity.name + "(ID:" + comp_entity.ID + ")";		emp.comp = comp_entity;			emp.birthday = new Date();		emp.save();	}});